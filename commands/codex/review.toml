description = "Run a local Codex review against the current workspace and return the output verbatim."
prompt = """
Run the local Codex review command for the current workspace and return its stdout verbatim with no additional commentary.

!{node "${extensionPath}${/}plugins${/}codex${/}scripts${/}gemini-command.mjs" review "${workspacePath}" {{args}}}
"""
