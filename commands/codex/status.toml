description = "Show local Codex background review status for the current workspace and return the output verbatim."
prompt = """
Run the local Codex status command for the current workspace and return its stdout verbatim with no additional commentary.

!{node "${extensionPath}${/}plugins${/}codex${/}scripts${/}gemini-command.mjs" status "${workspacePath}" {{args}}}
"""
